---
export async function getStaticPaths() {
  const posts = await Astro.glob('./*.md')

  return posts.map(post => ({
    params: {
      slug: post.frontmatter.slugName
    },
    props: {
      post
    },
  }))
}

console.log({ posts })

const { Content } = Astro.props.post
---
<article>
  <Content/>
</article>